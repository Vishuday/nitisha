json.vifmember do
  json.id @vifmember.id
  json.interface @vifmember.interface
  json.virtualinterface @vifmember.virtualinterface
  json.created_at @vifmember.created_at
  json.updated_at @vifmember.updated_at
end
